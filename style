button {
  border-radius: 8px;
  border: 1px solid transparent;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  font-family: inherit;
  background-color: #1a1a1a;
  cursor: pointer;
  transition: border-color 0.25s;
}
button:hover {
  border-color: #646cff;
}
button:focus,
button:focus-visible {
  outline: 4px auto -webkit-focus-ring-color;
}


<div className="h-36 w-full bg-gradient-to-r from-purple-400/100 via-pink-500/25 to-red-500/25 rounded p-1 mt-4">
        <div class="flex h-full w-full items-center justify-center bg-gray-800 rounded-sm">
          <h1 class="text-2xl font-black text-white">the quick brown fox jumps over the lazy dog</h1>
        </div>
      </div>

      <div className={`
        w-32 h-9 | text-base font-bold p-6 mt-4 rounded-lg
        flex justify-center items-center
        bg-gradient-to-b from-green to-green/5
        active:translate-y-1 active:duration-75
      `}>
        <button className={`  
          h-full w-full flex items-center justify-center bg-red-500
        `}>
          Staking</button>
      </div>